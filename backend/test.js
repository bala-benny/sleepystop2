const express = require('express');
const port = 3000;

const app = express();
  console.log(`Example app listening on port ${port}`);

  console.log(`Example app listening on port ${port}`);

app.get('/', (req, res) => {
  res.send('Hello World!');
});
  console.log(`Example app listening on port ${port}`);

app.listen(port, () => {
  console.log(`Example app listening on port ${port}`);
});


  console.log(`Example app listening on port ${port}`);

