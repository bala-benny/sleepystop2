# SleepyStop Frontend (Vite)

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Build:

```bash
npm run build
npm run preview
```

This is a minimal vanilla JS + Vite scaffold. I can copy existing assets or migrate UI components from the original if you want.
